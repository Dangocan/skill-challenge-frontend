import { StyledMainContainer } from "./style";

interface TMainContainerProps extends React.HTMLAttributes<HTMLInputElement> {
  labeled?: boolean;
}

function MainContainer({ labeled }: TMainContainerProps) {
  return <></>;
}

export default MainContainer;
